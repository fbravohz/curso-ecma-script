let hello = "hello";
let world = "world";

// Template literals
let epicPhrase = `${hello} ${world}!`;

console.log(epicPhrase);

// Multi-line strings

let lorem = "esto es un string \n" + "concatenado multiples veces";
console.log(lorem);

let loremMejorado = `Esta es una frase mejorada
con multiline
strings`;

console.log(loremMejorado);